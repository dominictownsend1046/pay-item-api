PORT=3000
MASTER_KEY=changeme_to_a_random_32_char_string

# Seed only in dev/testing
SEED_DEFAULT_USER=true
DEFAULT_USER=admin
DEFAULT_PASSWORD=changeMeNow123!

# Registration is disabled by default
ALLOW_REGISTRATION=false

# Token store: memory (default) or redis
TOKEN_STORE=memory
# REDIS_URL=redis://default:password@localhost:6379/0

# Postgres connection (Render format works)
DATABASE_URL=postgresql://payitem:payitem@localhost:5432/payitem?schema=public
